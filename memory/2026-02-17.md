# 2026-02-17

## Beflora — счётчики аналитики
- Добавил VK Pixel (id: 3742216) и Яндекс.Метрику (id: 98876779) на app.beflora.ru
- Файлы: `_vkpixel.html.erb`, `_counters.html.erb` в layouts
- Подключены в `app.slim` и `site.slim` внутри `if Rails.env == 'production'`
- Проблема была: файлы были `.html` без `.erb` — Rails не рендерил как partials
- После переименования + `kill -USR2` unicorn — заработало
- Клиент: Михаил Хабиров (@mikhail_khabirov)

## Aeza VPS — OpenClaw 24/7
- Создал сервер на Aeza (Амстердам, NLs-1): **62.60.244.130**
- 1 vCPU, 2GB RAM, 30GB NVMe, 593₽/мес
- Логин: root / OyaNyzC7VPg7
- Установил Node.js 22 + OpenClaw
- Systemd сервис: `/etc/systemd/system/openclaw.service`
- ENV: `ANTHROPIC_API_KEY` в systemd unit

## Второй бот @klaudii2bot
- Telegram бот на сервере: 7774098611:AAH5u-nlYhKK7wqat55ajQCRDUqwKTMU6zs
- dmPolicy: open, allowFrom: ["*"]
- Группа "Клешни" добавлена в allowedGroups: -1003817897092

## Git sync для workspace
- Репо: https://github.com/Kirill-found/openclaw-workspace (public)
- Синхронизируется: MEMORY.md, memory/, AGENTS.md, SOUL.md, USER.md, TOOLS.md
- Сервер pull каждые 10 мин через cron: `/etc/cron.d/openclaw-sync`
- Mac push вручную или через scripts/sync-workspace.sh

## Схема использования ботов
- @whoispog_bot (Mac) — сложные задачи, браузер, проекты, полный функционал
- @klaudii2bot (сервер) — 24/7 доступность, простые задачи, напоминания
- Оба видят одну память через git sync

## Telegram группы — конфигурация
- Группа "Клешни": chat_id `-1003817897092`
- Правильный ключ в конфиге: `groups` (объект), НЕ `allowedGroups`
- Формат:
```json
"groups": {
  "-1003817897092": {
    "requireMention": true
  }
}
```
- **Важно для ботов в группах**: нужно отключить Privacy Mode в @BotFather (`/setprivacy` → Disable), иначе бот не получает сообщения
- После изменения Privacy Mode — удалить бота из группы и добавить заново
